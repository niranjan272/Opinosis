opnosis
=========
File name || objective

edgelist.txt || contain list of nodes that have edges between them(list is redundant). Output from hdfs. Input to edge_strength.py

edge_strength.py || Inputs edgelist.txt and outputs list of nodes having edges between them but with edges between same nodes being clubbed 			    together 
